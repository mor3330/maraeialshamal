-- ══════════════════════════════════════════════════════
-- تحديث sync_agent إلى الإصدار 3.1
-- الإصلاحات:
--   1. DocumentTypeId IN (1, 2, 3, 4)  ← كان (2, 4) فقط مما أفقد فواتير نوع 1
--   2. إضافة حقل discount في بيانات الفاتورة المُرفوعة
-- التأثير: جميع الأجهزة تتحدث تلقائياً في أقل من 10 دقائق
-- ══════════════════════════════════════════════════════

-- احتفظ بنسخة احتياطية من الإصدار الحالي قبل التحديث
-- (اختياري - يمكن تخطيه)
-- INSERT INTO sync_agent_history SELECT * FROM sync_agent;

-- تحديث الإصدار فقط (السكريبت يُرفع بشكل منفصل عبر لوحة الإدارة)
UPDATE sync_agent
SET version = '3.1',
    updated_at = NOW()
WHERE id = (SELECT id FROM sync_agent ORDER BY id LIMIT 1);

-- إذا لم يوجد سجل بعد، أنشئه
INSERT INTO sync_agent (version, script_content, updated_at)
SELECT '3.1', '', NOW()
WHERE NOT EXISTS (SELECT 1 FROM sync_agent);

-- ══════════════════════════════════════════════════════
-- ملاحظة مهمة: بعد تشغيل هذا الـ SQL، يجب رفع
-- محتوى sync.py الجديد (v3.1) إلى عمود script_content
-- في جدول sync_agent حتى تتحدث الأجهزة تلقائياً.
--
-- يمكن رفعه من لوحة Supabase → Table Editor → sync_agent
-- أو عبر API من صفحة الإعدادات في لوحة الإدارة.
-- ══════════════════════════════════════════════════════
